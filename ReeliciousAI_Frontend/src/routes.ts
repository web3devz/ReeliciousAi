export const PUBLIC_ROUTES = ['/'];

export const AUTH_ROUTES = [
  '/auth/sign-in',
  '/auth/sign-up',
];

export const AUTH_API_PREFIX = '/api/auth';

export const DEFAULT_AUTH_REDIRECT = '/dashboard';

export const DEFAULT_AUTH_PAGE = '/auth/sign-in';
